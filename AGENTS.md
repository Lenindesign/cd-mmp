# Agent Notes

- Vehicle and deal image failures must render a text-only missing image state. Do not use generated SVG fallback images, decorative car illustrations, or unrelated vehicle photos as fallbacks.
